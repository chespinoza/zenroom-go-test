box: golang

build:
  steps:
    - setup-go-workspace
    - wercker/glide-install@3.0.1
    - glide-install
    - script:
        name: test
        code: |
          go version
          go build
          ./zenroom-go-test