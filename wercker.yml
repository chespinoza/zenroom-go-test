box: golang

build:
  steps:
    - setup-go-workspace
    - skidder/glide-install@1.0.3
    - script:
        name: test
        code: |
          go version
          glide install
          go build
          ./zenroom-go-test